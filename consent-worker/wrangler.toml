name = "meda-consent"
main = "src/index.js"
compatibility_date = "2026-09-20"

# No API tokens and no secrets. Deploy with `npx wrangler login`, then `npx wrangler deploy`.
# The old public /stats token is removed. That URL no longer returns data.

[triggers]
crons = ["15 3 * * *"]

[[d1_databases]]
binding = "DB"
database_name = "meda-consent"
database_id = "1f4490cf-dc82-49c6-aa99-c979b7608d79"

[vars]
ALLOWED_ORIGINS = "https://g5kjd9v7cf-boop.github.io,https://meda-vermittlung.de,https://www.meda-vermittlung.de"

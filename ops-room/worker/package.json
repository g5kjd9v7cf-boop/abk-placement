{
  "name": "ops-room-worker",
  "version": "1.0.0",
  "description": "Ops Room — shared coordination backend for multi-agent operations (Cloudflare Worker + D1).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "schema:local": "wrangler d1 execute ops-room --local --file=./schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.112.0"
  }
}
